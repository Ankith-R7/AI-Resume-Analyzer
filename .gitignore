# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
ENV/
env/

# Streamlit
.streamlit/config.toml (except if committed, wait, we want to keep config.toml since we customized it, so we can ignore config.toml? No, keep config.toml!)

# IDEs
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
